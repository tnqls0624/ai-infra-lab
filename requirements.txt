# 직접 사용하는 최상위 패키지만 명시 (버전 미고정 → 파이썬 버전에 맞게 자동 설치)
# numpy, scipy, ipython, jupyter_core 등 나머지는 아래 패키지가 의존성으로 자동 설치함

torch          # 딥러닝 프레임워크
torchvision    # 이미지 데이터셋 / 전처리 (pillow 포함)
scikit-learn   # 전통 ML + 수치 도구 (numpy, scipy 포함)
jupyterlab     # 노트북 실험 환경 (ipython, ipykernel 등 포함)
